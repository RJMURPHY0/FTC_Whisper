# Virtual environment
venv/

# Claude Code session data
.claude/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# SENSITIVE — never commit real credentials or session tokens
config.json
.session
.env

# Runtime / generated files
.ftc_pid
app_log.txt
app_err.txt
app_log_test.txt
debug_log.txt
startup_debug.txt
startup_test.txt

# Build / packaging artefacts (outputs only — ftc_whisper.spec is kept)
build/
dist/
*.egg-info/

# Claude Code assistant memory (not project source)
memory/

# Build logs + dream/memory backup dumps (local artefacts, not source)
build_*.log
memory-backup-dream-*/
backup-dream-*/
